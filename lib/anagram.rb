class Anagram
  attr_accessor :word
  def initialize(word)
    @name = word
  end

  def matches(collection)
    function isAnagram(a,b) {
    return new word([a,b].map(x => x.split('').sort().join(''))).size === 1;
}
  end
end
